institution: 00-example-university
maintainers:
  # Two maintainers, each showing a different optional contact field: a GitHub
  # handle on one, an email on the other. Both fields are optional; `name` alone
  # is enough.
  - name: Jordan Lefebvre
    github: jlefebvre-example
  - name: Priya Nair
    email: priya.nair@example.edu
# default_license omitted: falls back to the repository-wide default, CC-BY-SA-4.0.
# CPSC313 relies on that fallback; ECE450 sets its own course-level license instead,
# so between them the whole license cascade is on display.
