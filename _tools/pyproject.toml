[project]
name = "rvi-education-tools"
version = "0.1.0"
description = "Repository automation for RVI Education Central."
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "jsonschema>=4.21",
    "click>=8.1",
]

[tool.uv]
package = false
